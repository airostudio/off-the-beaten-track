import { VerticalPage } from '@/components/travel-products/VerticalPage';

export default function HotelsPage() {
  return (
    <VerticalPage
      category="hotel"
      title="Hotels"
      intro="Curated stays to go with your flight. Members see negotiated rates where a genuine one exists."
    />
  );
}
